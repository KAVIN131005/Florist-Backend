# Florist Backend - Production MySQL Configuration
spring.application.name=florist-backend

# MySQL database configuration
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}

# JPA/Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=false

# Server configuration
server.port=${PORT:8081}

# JWT configuration
app.jwt.secret=k7US25ehafHtiLP9DtYkfq539IiC8qCaDEi1LyT6WflU1a0+W0//svx0kPyWebr0pJmUjvY/4skyRk+KdhnK9Q==
app.jwt.expiration-ms=86400000

# CORS configuration
app.cors.allowed-origins=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000,http://127.0.0.1:5173,http://localhost:8081,https://florist-backend.vercel.app,https://florist-backend.onrender.com

# Admin seeder configuration
app.admin.email=${ADMIN_EMAIL:admin@florist.com}
app.admin.password=${ADMIN_PASSWORD:admin123}
app.admin.name=${ADMIN_NAME:Florist Admin}

# Actuator endpoints
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always



app.razorpay.key.id=${RAZORPAY_KEY_ID}
app.razorpay.key.secret=${RAZORPAY_KEY_SECRET}
